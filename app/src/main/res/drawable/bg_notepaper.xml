<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- ✨ Arka plan rengi (krem beyaz) -->
    <item>
        <shape android:shape="rectangle">
            <solid android:color="@color/background_paper" />
        </shape>
    </item>

    <!-- 📘 Çizgiler -->
    <item>
        <shape android:shape="rectangle">
            <size android:height="32dp" />
            <stroke android:width="1dp" android:color="@color/line_gray" />
        </shape>
    </item>

</layer-list>
